#!/bin/bash

#install package that not available because of AUR 
sudo snap install --edge vale
sudo snap install nuspell
sudo snap install languagetool
sudo apt  install -y lua-check
sudo apt  install -y lolcat
